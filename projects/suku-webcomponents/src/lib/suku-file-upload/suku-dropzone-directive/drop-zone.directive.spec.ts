import { SukuDropZoneDirective } from './drop-zone.directive';

describe('DropZoneDirective', () => {
  it('should create an instance', () => {
    const directive = new SukuDropZoneDirective();
    expect(directive).toBeTruthy();
  });
});
